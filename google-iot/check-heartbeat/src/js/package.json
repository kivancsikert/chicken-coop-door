{
  "name": "check-heartbeat",
  "version": "0.0.1",
  "dependencies": {
    "@google-cloud/firestore": "^4.12.2",
    "@slack/webhook": "^6.0.0"
  },
  "scripts": {
    "start": "gcloud functions deploy checkHeartbeat --runtime nodejs14 --memory 128mb --trigger-http --set-env-vars SLACK_URL=$SLACK_URL,TIMEOUT=$TIMEOUT --region europe-west1"
  }
}
